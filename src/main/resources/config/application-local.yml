logging:
  level:
    io:
      mend:
        seed: DEBUG
spring:
  devtools:
    livereload:
      enabled: true
    restart:
      enabled: true
management:
  endpoints:
    access:
      default: unrestricted
    web:
      exposure:
        include: '*'
