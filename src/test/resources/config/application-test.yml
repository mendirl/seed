logging:
  config: classpath:logback.xml
spring:
  main:
    banner-mode: 'off'
  datasource:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    hikari:
      maximum-pool-size: 2
    password: ''
    url: jdbc:tc:postgresql:18.2:///seed4jSampleApplication?TC_TMPFS=/testtmpfs:rw
    username: seed4jSampleApplication
  liquibase:
    contexts: test
